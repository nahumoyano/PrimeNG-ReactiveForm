import { CambiarColorEnabledPipe } from './cambiar-color-enabled.pipe';

describe('CambiarColorEnabledPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarColorEnabledPipe();
    expect(pipe).toBeTruthy();
  });
});
